import 'package:flutter/material.dart';

class BigBtnWidget extends StatelessWidget {
  final String? text;
  final double? width;
  final double? height;
  const BigBtnWidget({super.key, this.width, this.height, required this.text});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
